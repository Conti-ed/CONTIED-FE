export type KeywordType = {
  id?: number;
  name: string;
};

export type SongType = {
  id?: number;
  title: string;
  artist: string;
  releaseDate?: string;
  youtubeUrl?: string;
  lyrics?: string;
};
